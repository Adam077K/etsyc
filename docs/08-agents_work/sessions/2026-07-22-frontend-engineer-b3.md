---
role: frontend-engineer · task: B3 world unfold · date: 2026-07-22 · tier: full
branch: feat/b3-world-unfold · status: COMPLETE (awaiting QA-Lead + E5 eyes-on sign-off)
---
Shipped the unfold in 4 commits: E5 hero identity line (statement → name leads the caption beneath; absent → nameplate unchanged; `/preview?statement=off` toggle; flipped the pre-E5 assertion), §3.3 choreography (three bands on `--ease-cinematic`, 900 ms hard cap, 70 ms nearest-to-film waves with ~1.3× parallax, atmosphere breathes out 340–900, reverse = 702 ms derived, zero new tokens), deep-linkable `/w/[handle]` with the engine WORLD_OPEN read (pin via `videos.id`; any engine failure opens the world unpinned — never blocks the film), and the GROWN→WORLD_OPEN same-source persistence suite (Film Layer node identity, front buffer untouched, pause never called; mutation-verified red/green).
No-flattening verified live against **hollowgrain** (curated/light, fluid, Fraunces, r16) and **isoldeglass** (custom/dark, liquid, sharp r0) — different layout, tokens, atmosphere, motion. Reduced motion live-probed: `delay 0s, duration 1e-05s, transform none`, film playing.
Eyes-on: both hero variants captured at 1440 under real themes (screenshots in session scratchpad `b3-shots/`); seed media are poster gradients, not live footage — motion itself still needs the D12 clips.
Rebased onto `feat/b1a-feed-data`; converged on canonical identity: `kol_sid` (proxy-minted; `resolveFeedSessionId` validates) + `kol_ring` (`FEED_RING_COOKIE`), no route-local cookie constants. Live-probed: sid minted on `/w`, kept on second hit, legacy name gone.
Gates (post-rebase): typecheck 0 · lint clean · 658/658.

## Gate-2 fix cycle (same day)
Merged main @578a7ce (R1/R2/R3 rulings). Fixed the two gate-B P1s at the mechanism: the chrome band (`.kol-hero-chrome`) now paints solid `--scrim-strong` (ground 40% oklab→black) under every set line with a reserved fade above, bottom-anchored inside the overflow-hidden frame — coverage grows WITH the text, so over-film contrast is `contrast(--on-media, --scrim-strong)`, footage-independent: worst case sunbaked-light 9.07:1 (was 3.78 caption / 1.04 line-1), ≥7:1 target / ≥5.5 binding floor locked across all palettes×modes + custom (`scrim-strong.test.ts`). The ≤48-char budget is dead: `fitStatementScale` measures set lines (≤3 after balance) + band cap (≤0.8 frame) in the actual rect (sena 44-chars now sets 2 lines @69px). Mutation-verified both ways (fit loop forced-accept → 4 red; scrim mix weakened → 5 red; restored → green). R1 nameplate: `strokeClass` per pairing → `--nameplate-size/-weight/-tracking` (warm-serif modulated 700/display-hero; others+custom uniform 600/display); renderer reads vars only. Band ruling: 4:5 below `sm` (film exposed 0.547/0.606 ≥0.5 at 375px), 8cqi + 2-line clamp w/ ellipsis mobile, caption `mt-4 md:mt-10` (measured descender→cap 51.6–65.4px at 1440). Panel top inset 16/32px; caption name lead +100 weight; `/w` craft select dropped. isoldeglass "stray band" diagnosed NOT a theme leak — it is the atmosphere block's designed color-wash (`accent 10% oklab` mid-stop); removal is a Design-Lead call, reported not shipped. All contrast numbers measured against placeholder/synthetic footage — I5 close-out still needs re-verification on real D12 clips.
Gates: typecheck 0 · lint clean · 686/686 unit · 3/3 e2e (port now env-settable — a stale :3000 server was silently getting tested).

## Reconciliation with fix/b3-preview-film (same day)
Ported `26b9583` (FilmLayer cold-mount 0×0 fix — film now PAINTS in captures for the first time; production /w defect) and `b1ebd1d` (customPairing.strokeClass optional enum + design-system §3; StrokeClass single-sourced in store-config/types, tokens re-exports). Conflicts: tokens.ts (kept my nameplateRegisters+pairing tags, took their single-sourced type), custom.ts (kept MINE — --scrim gradient contract + --scrim-strong — wired only `pairing.strokeClass ?? "uniform"`; their whole-file version briefly broke rails/scrim-strong suites, caught and restored), rails.test.ts (took their additions). REMOVED the withdrawn 2-line clamp (`fd427ba`) — it truncated text fitStatementScale had just proven fits ≤3 lines; test now BANS -webkit-line-clamp. Recapture 375: complete 3-line statement, no ellipsis, film painted (343×429), band coverage 0.512 of frame (0.488 exposed — the honest un-clamped number; the top 41px of the band is translucent fade, so visually film shows through more).
Gates: typecheck 0 · lint clean · 690/690.
